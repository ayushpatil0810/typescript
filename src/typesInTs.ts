// type inference -> TS automatically know the type
let car = "hyundai";
let cups = Math.random() > 0.5 ? 10 : 5;

//type annotation -> explicitly mentioning types
let name: string = "Ayush";
name = "MyName";
